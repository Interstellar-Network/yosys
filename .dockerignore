# whitelist instead of blacklist
# that way COPY is properly cached which is useful when dev/testing the dockerfile

# Ignore everything
*

# Allow files and folders with a pattern starting with !
!build/yosys-*-Linux.deb
!tests/deb